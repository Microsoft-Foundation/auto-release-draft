name: 'Auto release draft'
description: 'Draft a github release with the change introduced by a newly created version tag.'
author: 'Vinod Soni'
output:
  release-url:
  description: 'The URL of the GitHub release that was drafted.'
runs:
  using: 'node16'
  main: 'dist/index.js'
